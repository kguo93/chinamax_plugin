# repo-map — .codex-plugin/

Codex plugin metadata.

- `plugin.json` — Codex manifest for the `chinamax` plugin, version `0.4.5`,
  exposing the shared Runtime, `skills/` interface, and Codex-specific
  `hooks/codex-hooks.json` registration.
