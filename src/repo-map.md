# repo-map — src/

The src-layout root. It carries no source of its own; everything lives in the package.

- `chinamax/` — the Runtime package: Profile resolution, provider client, agent loop, tools, Thread transcript, job spec, CLI seam. See `chinamax/repo-map.md`.
