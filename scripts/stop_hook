#!/usr/bin/env bash
# Stop hook shim: resolve the interpreter, then run the entrypoint.
# stdin (the hook event JSON) and stdout (the systemMessage JSON) pass verbatim.
set -euo pipefail
CHINAMAX_SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]:-$0}")" && pwd)"
# shellcheck source=scripts/_interpreter.sh
. "${CHINAMAX_SCRIPT_DIR}/_interpreter.sh"
chinamax_exec chinamax.hooks.stop "$@"
